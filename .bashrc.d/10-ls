if [[ $platform == 'linux' ]]; then
   alias ls='ls --color=auto --literal --group-directories-first'
elif [[ $platform == 'darwin' ]]; then
   alias ls='ls -G'
fi

alias l='ls'
alias ll='ls -l'
alias lla='ls -lA'
