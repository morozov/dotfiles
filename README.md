Add and uncomment the following to your `.bashrc`

```
# if [ -d ~/.bashrc.d ]; then
#     for x in ~/.bashrc.d/* ; do
#         test -f "$x" || continue
#         test -x "$x" || continue
#         . "$x"
#     done
# fi
```
